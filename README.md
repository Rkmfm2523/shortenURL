URL Shortener Service (Go)
Микросервис для сокращения URL-адресов с REST API, написанный на Go с использованием чистой архитектуры.

📦 Функциональность
Сокращение URL: POST /url для создания коротких ссылок

Валидация: Автоматическая проверка корректности URL

Кастомизация: Возможность указать свой алиас

Генерация: Автогенерация уникального алиаса (10 символов)

Хранение: SQLite база данных с persistent storage

Логирование: Структурированное логирование с разными уровнями

🛠 Технологический стек
Язык: Go 1.25.5

База данных: SQLite

Веб-фреймворк: Chi Router

Валидация: go-playground/validator

Конфигурация: Cleanenv (YAML + env vars)

Логирование: slog (структурированное)

Контейнеризация: Docker

🚀 Быстрый старт
Требования
Go 1.25+

SQLite3

Docker (опционально)

Установка и запуск
Клонирование и настройка

bash
git clone <repository-url>
cd shortener
Настройка конфигурации

bash
export CONFIG_PATH="./config/local.yaml"
# Или создайте файл .env с этой переменной
Запуск сервера

bash
go run cmd/shortener/main.go
Проверка работы

bash
curl -X POST http://localhost:8087/url \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com"}'

🔧 Конфигурация
Основные параметры (config/local.yaml)
yaml
env: "local"                 # Режим работы (local/dev/prod)
storage_path: "./storage/storage.db"  # Путь к БД
http_server:
  address: "localhost:8087"  # Адрес сервера
  timeout: 5s                # Таймаут запросов
  idle_timeout: 60s          # Таймаут простоя
Переменные окружения
CONFIG_PATH: путь к конфигурационному файлу

ENV: режим работы (переопределяет config)

📡 API Endpoints
POST /url - Создание короткой ссылки
Запрос:

json
{
  "url": "https://example.com",
  "alias": "myalias"  # опционально
}
Успешный ответ:

json
{
  "status": "OK",
  "alias": "myalias"
}
Ошибка:

json
{
  "status": "Error",
  "error": "сообщение об ошибке"
}
🧪 Режимы работы
Локальный режим (env: local)
Текстовые логи с DEBUG уровнем

Локальная SQLite база

Адрес: localhost:8087

Продакшен режим (env: prod)
JSON логи с INFO уровнем

Усиленная безопасность

Оптимизированные настройки

🐳 Docker запуск
bash
# Сборка образа
docker build -t url-shortener .

# Запуск контейнера
docker run -p 8087:8087 \
  -v ./storage:/app/storage \
  -e CONFIG_PATH="/app/configs/prod.yaml" \
  url-shortener
🗄 База данных
Схема таблицы url
sql
CREATE TABLE IF NOT EXISTS url(
  id INTEGER PRIMARY KEY,
  alias TEXT NOT NULL UNIQUE,
  url TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_alias ON url(alias);
Методы хранилища
SaveURL(url, alias) - сохранение ссылки

GetURL(alias) - получение оригинала по алиасу

DeleteURL(alias) - удаление ссылки

🔒 Безопасность
Валидация URL формата

Защита от SQL-инъекций (prepared statements)

Обработка race conditions

Потокобезопасные операции

📈 Логирование
Структурированные логи в формате:

local: человекочитаемый текст с DEBUG уровнем

prod: JSON формат с INFO уровнем

Логи включают:

Request ID для трассировки

Время выполнения

Коды ошибок

Данные запросов

🧩 Расширение функциональности
Планируемые улучшения:
Метрики: Prometheus метрики

Кеширование: Redis для горячих ссылок

RPC: gRPC интерфейс

Кластеризация: Поддержка нескольких инстансов

Админка: Веб-интерфейс управления

🐛 Отладка
Проверка здоровья
bash
curl http://localhost:8087/health
Просмотр логов
bash
# Локальный режим
tail -f storage/app.log

# Docker
docker logs <container_id>
Тестирование
bash
# Запуск тестов
go test ./...

# Бенчмарки
go test -bench=. ./...
📝 Особенности реализации
Архитектурные решения:
Чистая архитектура: Разделение слоёв

Dependency Injection: Внедрение зависимостей

Interface-based design: Контракты через интерфейсы

Error wrapping: Детализированные ошибки с контекстом

Оптимизации:
Connection pooling для БД

Prepared statements переиспользование

Минимальные аллокации памяти

Efficient string generation

